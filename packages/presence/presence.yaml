group:
  rooms:
    - binary_sensor.kitchen_occupied
    - binary_sensor.living_room_occupied
    - binary_sensor.office_occupied
    - binary_sensor.guest_bedroom_occupied

  occupancy:
    - binary_sensor.kitchen_motion

  room_presence:
    - sensor.ruben_room

#region template
template:
  - binary_sensor:
      - name: kitchen_occupied
        icon: mdi:account-group
        state: >
          {{ states.binary_sensor | 
          selectattr('entity_id', 'in', area_entities('keuken')) |
          rejectattr('attributes.device_class', 'undefined') |
          selectattr('attributes.device_class', 'search', '(occupancy|motion|door)') | 
          selectattr('state', 'eq', 'on') | 
          map(attribute='entity_id') |
          list | count > 0}}
      - name: living_room_occupied
        unique_id: living_room_occupied
        icon: mdi:account-group
        state: >
          {{ (expand('group.room_presence') 
            | selectattr('state', 'eq', 'living_room') 
            | list 
            | count >= 1 )
            or 
            (states.binary_sensor | 
                selectattr('entity_id', 'in', area_entities('woonkamer')) |
                rejectattr('attributes.device_class', 'undefined') |
                selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
                selectattr('state', 'eq', 'on') | 
                map(attribute='entity_id') | list | count > 0 )}}
      - name: bedroom_occupied
        icon: mdi:account-group
        state: >
          {{ states.binary_sensor | 
          selectattr('entity_id', 'in', area_entities('slaapkamer')) |
          rejectattr('attributes.device_class', 'undefined') |
          selectattr('attributes.device_class', 'search', '(occupancy|motion|door)') | 
          selectattr('state', 'eq', 'on') | 
          map(attribute='entity_id') |
          list | count > 0}}
      - name: office_occupied
        icon: mdi:account-group
        state: >
          {{ states.binary_sensor | 
          selectattr('entity_id', 'in', area_entities('werkkamer')) |
          rejectattr('attributes.device_class', 'undefined') |
          selectattr('attributes.device_class', 'search', '(occupancy|motion|door)') | 
          selectattr('state', 'eq', 'on') | 
          map(attribute='entity_id') |
          list | count > 0}}
      - name: evi_bedroom_occupied
        icon: mdi:account-group
        state: >
          {{ states.binary_sensor | 
          selectattr('entity_id', 'in', area_entities('evi')) |
          rejectattr('attributes.device_class', 'undefined') |
          selectattr('attributes.device_class', 'search', '(occupancy|motion|door)') | 
          selectattr('state', 'eq', 'on') | 
          map(attribute='entity_id') |
          list | count > 0}}
      - name: guest_bedroom_occupied
        icon: mdi:account-group
        state: >
          {{ states.binary_sensor | 
          selectattr('entity_id', 'in', area_entities('logeerkamer')) |
          rejectattr('attributes.device_class', 'undefined') |
          selectattr('attributes.device_class', 'search', '(occupancy|motion|door)') | 
          selectattr('state', 'eq', 'on') | 
          map(attribute='entity_id') |
          list | count > 0}}

#region script
script:
  #region room presence
  get_room_presence:
    sequence:
      - variables:
          occupied_rooms: >
            {% set rooms = ['living_room'] %}
            {% if is_state('input_boolean.audible_notifications', 'on') %}
            {% if expand('group.rooms')
            | selectattr('state', 'eq', 'on') | list | count >= 1 %}
            {% set rooms = expand('group.rooms')
            | selectattr('state', 'eq', 'on')
            | sort(attribute='last_changed', reverse=True)
            | map(attribute='name') | list %}
            {%- endif -%}
            {% else -%}
            {%- if expand('group.rooms')
            | rejectattr('name', 'search', 'bedroom')
            | selectattr('state', 'eq', 'on') | list | count >= 1 %}
            {%- set rooms = expand('group.rooms')
            | rejectattr('name', 'search', 'bedroom')
            | selectattr('state', 'eq', 'on')
            | sort(attribute='last_changed', reverse=True)
            | map(attribute='name')| list %}
            {%- endif -%}
            {% endif %}
            {{ rooms | replace('_occupied','') }}
          occupied_rooms_cnt: >
            {% if is_state('input_boolean.audible_notifications', 'on') %}
              {{ expand('group.rooms') 
                | selectattr('state', 'eq', 'on') | list | count }}
            {% else %}
              {{ expand('group.rooms') 
                | rejectattr('name', 'search', 'bedroom') 
                | selectattr('state', 'eq', 'on') | list | count }}
            {% endif %}
          room_presence: >-
            {"occupied_rooms":"{{occupied_rooms}}",
            "occupied_rooms_cnt":"{{occupied_rooms_cnt}}"}
      - stop: "ok, Im done."
        response_variable: "room_presence"
